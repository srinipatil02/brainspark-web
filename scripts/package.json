{
  "name": "brainspark-scripts",
  "version": "1.0.0",
  "description": "Upload scripts for BrainSpark questions",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
